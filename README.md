# Библиотека читшитов JetMetrics

## О проекте
Публичная галерея читшитов по аналитике (сейчас 101 штука, PDF/PNG), которая раньше жила в закрытой Notion-базе. Задача — пересобрать её в кастомную HTML-страницу, встраиваемую в сайт на Tilda, с возможностью управлять контентом без правки кода при каждом обновлении.

## Цели
- Открытая (пока без логина) библиотека читшитов с поиском и фильтрами по тегам
- Обновление силами Клода: новый читшит = новая запись в `data.json`, без визуальной админки
- Встраивание в существующий сайт на Tilda

## Подход
- Полностью статичный сайт: `index.html` + `style.css` + `app.js` + `data.json`, без бэкенда
- Файлы (PDF + PNG-превью) хостятся на GitHub Pages — публичный бесплатный хостинг с обновлением через git push
- Каждый читшит: заголовок, теги (можно несколько), превью (рендер первой страницы PDF), ссылка на PDF, ссылка на исходный пост в Telegram-канале @jetmetrics (где есть)
- Бренд-стиль JetMetrics: зелёный `#0E9C7D`, фиолетовый `#9370DB` как акцент
- В Tilda встраивается через `<iframe>` на GitHub Pages URL (быстрый вариант для демо); нативная вставка без iframe — доработка на потом

## Текущее состояние
- **Готово и работает:** сетка карточек, поиск по названию, фильтры по тегам (мультивыбор), лайтбокс с превью + кнопки «Скачать PDF» / «Читать пост в Telegram» / «Скопировать ссылку» (иконка), диплинки вида `?cs=cs-001`
- **Хостинг:** репозиторий `mary-jetmetrics/cheatsheets-library` на GitHub, Pages включён и отдаёт живую страницу — https://mary-jetmetrics.github.io/cheatsheets-library/
- **Теги:** 14 штук, расставлены вручную по каждому читшиту (свой черновой набор, не сверен с английской версией из Notion)
- **Ссылки на Telegram-посты:** сматчено 67 из 101 (канал @jetmetrics спарсен через `/tg-parse`, сопоставление сделано вручную по смыслу, не только текстовым совпадением — первая автоматическая версия ошибалась примерно в половине случаев)
- **Embed для Tilda:** готовый iframe-сниппет лежит в `tilda_embed_snippet.html`

## Следующие шаги
- Вставить `tilda_embed_snippet.html` в Tilda (HTML-код / Zero Block) для демо на созвоне
- Позже, без спешки: заменить iframe-embed на нативную вставку (без iframe, с наследованием шрифтов/фона страницы Tilda)
- **Обновить все PDF-читшиты — убрать неактуальные ссылки** (в футере читшитов зашиты ссылки вроде `jetmetrics.io/pro-workspace`, которые могли устареть) — отложено, не срочно
- Донабрать оставшиеся 34 ссылки на Telegram-посты вручную, если Мария вспомнит конкретные посты
- Сверить черновой набор тегов с английской версией из Notion, когда будет доступ

## История итераций

### 2026-07-03
- Собрана вся галерея с нуля: скопированы 101 PDF из `ру читшиты/`, сгенерированы превью через `pdftoppm`, вручную расставлены заголовки и теги
- Построен фронтенд: сетка, поиск, фильтры, лайтбокс — несколько раундов правок по фидбеку (шрифт лайтбокса наследовался неправильно, картинка обрезалась из-за flex-бага с процентной высотой, кнопки в футере выглядели одинаково)
- Спарсен Telegram-канал @jetmetrics (376 постов), вручную сматчено 67 читшитов с постами, добавлена кнопка «Читать пост в Telegram» и «Скопировать ссылку» (иконка, не текстовая кнопка — во избежание визуального дублирования с остальными кнопками)
- Создан репозиторий `mary-jetmetrics/cheatsheets-library`, включён GitHub Pages, сайт живой
- Подготовлен iframe-сниппет для встраивания в Tilda

## Решения и логика
- **Статика без бэкенда** — потому что Мария хотела «максимально просто, без своего сервера»; обновления идут через файл `data.json`, который готовит Клод
- **GitHub Pages, а не Tilda-загрузка или Google Drive** — обновление = положить файл и запушить, без ручной загрузки через интерфейс; Google Drive даёт капризные прямые ссылки на превью/PDF
- **Свой набор тегов вместо ожидания Notion-экспорта** — Notion был недоступен в сессии, Мария попросила не ждать и предложить свой вариант
- **Серия «How do we use Claude for our job» (7 карточек)** — показана как 7 отдельных карточек, а не одна с мини-каруселью — проще для MVP
- **Заголовки карточек** — оставлен номер статьи (`#044 —`), убран суффикс «RUS»
- **iframe вместо нативного embed для Tilda сегодня** — компромисс ради скорости (демо через час); нативная вставка отложена
